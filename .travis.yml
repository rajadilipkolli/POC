language: java

jdk:
  - oraclejdk11
  
services:
  - mongodb
  - redis-server
  - postgresql
 
script:
  - ./mvnw -T 1C clean verify
cache:
  directories:
    - '$HOME/.m2/repository'
    
after_success:
  # test coverage reporting
  - bash <(curl -s https://codecov.io/bash)
